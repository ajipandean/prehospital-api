runtime: nodejs
env: flex
service: canary

manual_scaling:
  instances: 1

network:
  name: default
  session_affinity: true

beta_settings:
  cloud_sql_instances: g-one-db:asia-northeast1:prehospital-db-canary
